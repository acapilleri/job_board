<% if @job.new_record? %>
  <%= link_to '&#8592; Back'.html_safe , new_job_step_path, class: :back %>
<% else %>
  <%= link_to '&#8592; Back'.html_safe , jobs_path, class: :back  %>
<% end %>

<%= render 'jobs/show'%>
<% if is_mine?(@job) %>
  <%= link_to 'Modifica', edit_job_step_path(@job) %>
| <%= link_to 'Cancella', @job, method: :delete, confirm: 'vuoi cancellare questo annuncio?' unless @job.new_record? %>
<% end %>

